// Just some random code for the purposes of the task.

// a random array
let randomArr = [1, 5, 3, 6, 10];

// log all elements multiplied by 5
randomArr.forEach((element) => {
  console.log(element * 5);
});
